# Serverless (Vercel) dependency set for the API.
#
# This is pyproject's core list minus four packages that cannot live in a serverless bundle:
#   spacy        — needs the en_core_web_sm model downloaded at build time (~50 MB + native)
#   weasyprint   — needs Pango/Cairo/GDK system libraries, which cannot be apt-installed here
#   playwright   — needs a Chromium download far beyond the bundle limit
#   browser-use  — only useful with Playwright, and pulls the langchain provider stack with it
#
# All four are imported lazily in the application, so the API starts without them and the
# features that need them report their absence instead of crashing. The container images in
# docker/ install the complete set and remain the full deployment.
#
# Keep in step with backend/pyproject.toml [project.dependencies].

fastapi>=0.111.0
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13.0
alembic-postgresql-enum>=1.3.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
redis[hiredis]>=5.0.0
arq>=0.26.0
litellm>=1.96,<2
portkey-ai>=1.6.0
langgraph>=1.2,<2
python-docx>=1.1.0
jinja2>=3.1.0
pypdf>=4.0.0
pdfplumber>=0.11.0
httpx>=0.27.0
aiofiles>=24.1.0
beautifulsoup4>=4.12.0
structlog>=24.1.0
prometheus-client>=0.20.0
tenacity>=8.3.0
exa-py>=1.1.0
PyJWT>=2.8.0
pwdlib[argon2]>=0.2.0
cryptography>=43.0.0
